<div class="container">
    <div class="form-tweet">
        <%= simple_form_for [ @user, @post ] do |f| %>
        <%= f.input :content %>
        <%= f.submit "Submit review", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
